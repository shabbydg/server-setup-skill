# Server Recon

A Claude Code subagent. See [agent.md](./agent.md) for its frontmatter (name, description, tools, model) and system prompt.

## Install

Copy `agent.md` into `.claude/agents/server-recon.md` in a project, or into `~/.claude/agents/server-recon.md` for global use.
