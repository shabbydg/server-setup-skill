# Remote Server Ops

A Claude Code Agent Skill. See [SKILL.md](./SKILL.md) for the trigger description and instructions.

## Install

Copy (or symlink) this directory into `.claude/skills/` in a project, or into `~/.claude/skills/` for global use.
